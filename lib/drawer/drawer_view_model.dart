import 'package:google_maps_flutter/google_maps_flutter.dart';
import 'package:geolocator/geolocator.dart';
Position? kLastLocation;

class DrawerViewModel {
  // final geolocatorPlatform = GeolocatorPlatform.instance;


  // LatLng _cameraposition =
  //     const LatLng(24.143587246187604, 120.68893067904283);
  // Future<LatLng> getMyLocation() async {
  //    final permission = await geolocatorPlatform.checkPermission();
  //   kLastLocation = await _openPosition();
  //   _currentPosition = await location.getLocation();
  //   _cameraposition = LatLng(
  //       _currentPosition.latitude ?? 0.0, _currentPosition.longitude ?? 0.0);
  //   return _cameraposition;
  // }
}
